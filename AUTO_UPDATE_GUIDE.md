# 🔄 MenuorgPrint 自动更新功能指南

## 🎯 **功能概述**

MenuorgPrint 现已集成完整的自动更新功能，支持：

- ✅ **启动时自动检查更新**
- ✅ **手动检查更新**
- ✅ **自动下载更新包**
- ✅ **用户确认后安装更新**
- ✅ **系统托盘更新入口**
- ✅ **应用内设置面板更新管理**

## 📱 **用户使用指南**

### 自动更新流程

1. **启动检查**
   - 应用启动后5秒自动检查更新
   - 仅在生产环境（已打包）中启用
   - 开发模式下跳过自动检查

2. **发现新版本**
   - 弹出更新对话框
   - 显示版本信息和更新内容
   - 三个选项：立即更新、稍后提醒、跳过此版本

3. **下载更新**
   - 显示下载进度窗口
   - 实时显示下载百分比和速度
   - 下载完成后提示安装

4. **安装更新**
   - 用户确认后重启应用
   - 自动应用新版本

### 手动检查更新

**方法一：系统托盘**
- 右键点击托盘图标
- 选择"🔄 检查更新"

**方法二：应用设置**
- 打开应用主界面
- 进入设置面板
- 找到"🔄 应用更新"部分
- 点击"检查更新"按钮

## 🛠️ **开发者配置指南**

### 发布新版本流程

1. **更新版本号**
   ```bash
   npm version patch  # 补丁版本 1.0.0 -> 1.0.1
   npm version minor  # 次版本 1.0.0 -> 1.1.0
   npm version major  # 主版本 1.0.0 -> 2.0.0
   ```

2. **构建并发布**
   ```bash
   # 设置GitHub Token
   set GH_TOKEN=your_github_token
   
   # 构建并发布到GitHub Releases
   npm run publish
   ```

3. **验证发布**
   - 检查GitHub Releases页面
   - 确认文件已上传：`MenuorgPrint-Setup-x.x.x.exe`

### 配置说明

**package.json 关键配置：**
```json
{
  "build": {
    "publish": [
      {
        "provider": "github",
        "owner": "cuiD2488",
        "repo": "menuorg-print"
      }
    ]
  },
  "dependencies": {
    "electron-updater": "^6.1.7"
  }
}
```

**环境变量要求：**
- `GH_TOKEN`: GitHub Personal Access Token
- 权限：repo（完整仓库权限）

## 📁 **文件结构**

```
├── src/
│   └── auto-updater.js           # 主进程自动更新管理器
├── renderer/
│   ├── js/
│   │   └── update-manager.js     # 渲染进程更新UI管理器
│   ├── css/
│   │   └── style.css            # 更新相关样式
│   └── index.html               # 引入更新脚本
├── main.js                      # 集成自动更新功能
├── preload.js                   # 暴露更新API
└── package.json                 # 更新配置和依赖
```

## 🔧 **功能特性**

### 自动更新管理器 (AutoUpdaterManager)

**主要功能：**
- 🔍 检查更新（启动时/手动）
- 📥 下载更新包
- 📊 显示下载进度
- 🔄 重启安装更新
- 💬 用户交互对话框
- 🔔 系统通知

**事件处理：**
- `checking-for-update`: 检查更新中
- `update-available`: 发现新版本
- `update-not-available`: 已是最新版本
- `download-progress`: 下载进度
- `update-downloaded`: 下载完成
- `error`: 错误处理

### 前端更新管理器 (UpdateManager)

**UI组件：**
- 📱 设置面板集成
- 🎯 实时状态显示
- 🎨 美观的按钮设计
- 📊 版本信息展示

**用户交互：**
- 检查更新按钮
- 下载更新按钮
- 安装更新按钮
- 状态指示器

## 🎨 **用户界面**

### 托盘菜单
```
┌─────────────────┐
│ 打开应用         │
│ 隐藏窗口         │
├─────────────────┤
│ 🚀 开机自动运行   │
├─────────────────┤
│ 🔄 检查更新      │ ← 新增
│ 退出应用         │
└─────────────────┘
```

### 设置面板
```
🔄 应用更新
┌─────────────────────────────┐
│ 当前版本: 1.0.0              │
│ 更新状态: 当前已是最新版本     │
│                             │
│ [检查更新] [下载更新] [安装更新] │
└─────────────────────────────┘
```

## ⚠️ **注意事项**

1. **网络要求**
   - 需要互联网连接检查更新
   - 自动从GitHub Releases下载

2. **权限要求**
   - 安装更新需要管理员权限
   - Windows可能弹出UAC确认

3. **开发环境**
   - 开发模式下自动更新功能被禁用
   - 只有打包后的应用才能使用更新功能

4. **备份建议**
   - 重要数据建议定期备份
   - 更新前系统会自动备份旧版本

## 🆘 **故障排除**

### 常见问题

**Q: 检查更新失败**
- 检查网络连接
- 确认GitHub可访问
- 查看控制台错误信息

**Q: 下载更新失败**
- 检查磁盘空间
- 确认防火墙设置
- 重试下载

**Q: 安装更新失败**
- 确认管理员权限
- 关闭杀毒软件临时
- 手动重启应用

### 日志查看

开发者控制台会显示详细的更新日志：
```
🔄 自动更新器已初始化
🚀 启动时检查更新...
🔍 正在检查更新...
✅ 发现新版本: 1.0.1
📥 下载进度: 50% (2.5MB/5.0MB)
✅ 更新下载完成: 1.0.1
🔄 正在重启并安装更新...
```

## 📞 **技术支持**

如有问题，请提供以下信息：
- 当前应用版本
- 操作系统版本
- 错误信息截图
- 控制台日志

---

**🎉 享受自动更新带来的便利！** 